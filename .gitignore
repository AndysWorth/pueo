# ==========================================
# 🔍 Audit Reports (local only — never committed)
# ==========================================
audits/

# ==========================================
# 🐳 Docker & Container Runtime Environment
# ==========================================
# Generated by setup.sh with user-specific SSH key path embedded — use docker-compose.yml.example as the reference template
docker-compose.yml
# Ignore local Docker compose overrides or local dev setups
docker-compose.override.yml
.docker/
.devcontainer/

# ==========================================
# 🐍 Python Runtime & Cache (Generated inside Container)
# ==========================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# ==========================================
# 🧪 Testing, Linting & Coverage Metrics
# ==========================================
.pytest_cache/
.mypy_cache/
.ruff_cache/
.bandit/
htmlcov/
.toc
.coverage
.coverage.*
coverage.xml
coverage.json
*.cover
*.py,cover

# ==========================================
# Runtime caches
# ==========================================
.cache/
nosetests.xml
pylintrc
.htmlcov/

# ==========================================
# 💻 IDEs & Environment Secrets (Host or Container mount)
# ==========================================
# Local config (from previous step)
config.yaml
secrets.yaml
.env
.venv
venv/
ENV/
env.bak/
venv.bak/

# macOS metadata
.DS_Store

# JetBrains / VS Code
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Obsidian workspace metadata
.obsidian/

# Claude Code user-specific local settings (project settings.json is committed)
.claude/settings.local.json

# Agent runtime state
# Legacy safety guards — runtime state now lives outside the repo in platform dirs (~/Library/...),
# but these entries protect against accidental commits when upgrading from an old layout.
pueo.pid
ha_agent_state.db
*.log
/hitl/
backups/
archives/
chromadb/
# user_tools/ is gone — dynamic tools now live in state_dir/tools/ (outside the repo)
