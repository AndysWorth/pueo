Analyze the following Home Assistant configuration YAML or log excerpt for issues:

$ARGUMENTS

Identify:
- Syntax errors or deprecated YAML schema
- Integration or component failures and their root causes
- Whether the issue is fixable via a config file change
- A corrected YAML snippet if applicable

Use the same diagnostic categories as `DiagnosticsReport` in `pueo/ha_agent_core.py`: severity (`NONE` / `LOW` / `MEDIUM` / `CRITICAL`), identified issues list, and recommended fix.
